<!DOCTYPE html>
<html>
<head>
  <title>Hydra</title>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
  <link href='http://fonts.googleapis.com/css?family=Exo:400,800' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Menu</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">Hydra</a>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to "Products", products_path %></li>
          <li><%= link_to "Personas", personas_path %></li>
          <li><%= link_to "User Stories", stories_path %></li>
        </ul>
        <p class="navbar-text navbar-right"></p>
        <% if user_signed_in? %>
          <p class="navbar-text navbar-right"><%= current_user.name %> | <%= link_to('Log out', destroy_user_session_path, :method => :delete) %></p>
        <% else %>
          <p class="navbar-text navbar-right"><%= link_to('Log in', new_user_session_path)  %></p>
        <% end %>
      </div>
    </div>
  </nav>
    <div class="container">
        <div class="row">
            <div class="col-lg 12">
              <%= yield %>
            </div>
        </div>
    </div>
<script type="text/javascript" charset="utf-8">
  $(document).ready(function() {
    $('#data-table').dataTable();
  } );
</script>
</body>
</html>
